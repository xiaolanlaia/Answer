<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.answer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".randomExamAnswer.RandomExamAnswerActivity" />
        <activity android:name=".myAccount.ManagerAccountActivity" />
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".randomExam.RandomExamActivity" />
        <activity android:name=".createRandomExam.CreateRandomExamActivity" />
        <activity android:name=".login.view.LoginActivity" />
        <activity
            android:name=".exam.ExamActivity"
            android:launchMode="singleTask" />
    </application>

</manifest>